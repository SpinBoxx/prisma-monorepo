{
	"name": "@test-turbo-repo/db",
	"version": "0.0.0",
	"devDependencies": {
		"prisma": "^6.19.0"
	},
	"dependencies": {
		"@prisma/client": "^6.19.0",
		"@prisma/extension-accelerate": "^2.0.2"
	},
	"scripts": {
		"db:generate": "prisma generate",
		"db:migrate": "prisma migrate dev --skip-generate",
		"db:deploy": "prisma migrate deploy"
	},
	"typings": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	}
}
